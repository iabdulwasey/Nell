/**
 * @nell/web
 *
 * The Next.js dashboard and web chat: task registry, live browser views, receipts,
 * approvals inbox, vault UI, memory browser, audit view, settings. The web app is
 * never an enforcement path — all security boundaries live in the core/policy
 * engine.
 *
 * Governed by: docs/architecture.md, docs/roadmap.md
 *
 * Status: scaffold stub. The Next.js app is initialized in v1.
 */

export const __stub = true;
