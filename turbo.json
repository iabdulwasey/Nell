{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "DATABASE_URL",
    "KERNEL_API_KEY",
    "MODEL_GATEWAY_API_KEY",
    "NELL_LICENSE_KEY",
    "NELL_LICENSE_PUBLIC_KEY",
    "NELL_OWNER_TELEGRAM_ID",
    "NODE_ENV",
    "SECRET_ENCRYPTION_KEY",
    "TELEGRAM_BOT_TOKEN"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["!.next/cache/**", ".next/**", "dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
