/**
 * @nell/aegis
 *
 * The policy engine and tool-executor chokepoint: spend-approval tokens, the provenance/untrusted-content gate, vault origin allowlist checks, the post-autofill taint machine, rate limits. Security is enforced here, not in prompts.
 *
 * Governed by: docs/security-model.md
 *
 * Status: scaffold stub. Implementation lands per the roadmap.
 */

// TODO(phase-0): implement per the referenced doc.

export const __stub = true;
