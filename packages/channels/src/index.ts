/**
 * @nell/channels
 *
 * The ChannelPort interface, the canonical idempotent message envelope, and per-channel renderers (Telegram, WhatsApp, iMessage, web, voice). Markdown never reaches a channel that can't render it.
 *
 * Governed by: docs/architecture.md
 *
 * Status: scaffold stub. Implementation lands per the roadmap.
 */

// TODO(phase-0): implement per the referenced doc.

export const __stub = true;
