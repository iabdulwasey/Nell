{
  "name": "@nell/agent",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude '**/*.live.test.ts' --passWithNoTests",
    "test:live": "RUN_LIVE_MODEL_TESTS=1 vitest run src/planner.live.test.ts"
  },
  "dependencies": {
    "@nell/aegis": "workspace:*",
    "@nell/browser": "workspace:*",
    "@nell/channels": "workspace:*",
    "@nell/durable": "workspace:*",
    "@nell/memory": "workspace:*",
    "@nell/shared": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vitest": "4.1.10"
  }
}
