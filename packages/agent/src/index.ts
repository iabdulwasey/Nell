/**
 * @nell/agent
 *
 * The coordinator/worker agent runtime: prompts, tool registry, the ModelRouter, and the briefing composer. The coordinator owns the relationship and never drives a browser or sees secrets; workers run one durable workflow per task.
 *
 * Governed by: docs/architecture.md
 *
 * Status: scaffold stub. Implementation lands per the roadmap.
 */

// TODO(phase-0): implement per the referenced doc.

export const __stub = true;
