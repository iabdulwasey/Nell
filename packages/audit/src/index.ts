/**
 * @nell/audit
 *
 * Append-only, hash-chained audit log of every consequential action, plus the verification CLI. GDPR erasure pseudonymizes subject refs while preserving the chain.
 *
 * Governed by: docs/security-model.md
 *
 * Status: scaffold stub. Implementation lands per the roadmap.
 */

// TODO(phase-0): implement per the referenced doc.

export const __stub = true;
