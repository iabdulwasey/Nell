/**
 * @nell/vault
 *
 * Encrypted secret vault (AES-256-GCM, per-item AAD binding, envelope keys) and secretless autofill: the model gets opaque handles; secrets are decrypted server-side and injected into the browser, never entering the model context.
 *
 * Governed by: docs/security-model.md
 *
 * Status: scaffold stub. Implementation lands per the roadmap.
 */

// TODO(phase-0): implement per the referenced doc.

export const __stub = true;
